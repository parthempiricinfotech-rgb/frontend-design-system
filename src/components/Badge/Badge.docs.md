# Badge

Category: status.

Variants: tone neutral, success, warning, danger; size compact and default.

Design contract: compact size keeps 8px horizontal padding. Current code uses 6px; no exception exists.
