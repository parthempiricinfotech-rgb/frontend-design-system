# Button

Category: actions.

Variants: `intent` primary, secondary, danger; `size` sm, md, lg; states default, hover, pressed, focus, disabled, loading.

Documented exception: the implementation uses a 3px focus ring for high contrast visibility. The Figma library still shows the older 2px visual token, but engineering accessibility guidance makes the 3px ring intentional.
