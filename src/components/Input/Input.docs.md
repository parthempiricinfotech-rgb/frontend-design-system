# Input

Category: forms.

States: default, focus, disabled, error, success.

Design contract: error state border should use `color/danger/500`. Current implementation uses `--color-danger-600`; no documented exception exists.
